apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: fcgames
  labels:
    app: users-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    name: http
  selector:
    app: users-api
